{% extends 'base.html.twig' %}

{% block title %}SKTTTTTT{% endblock %}

{% block body %}
    <div class="col-12">
        <div class="row">
            <div class="col-12">
                <div class="bck" style="border: 2px solid;">
                </div>
            </div>
        </div>
        <div>
            <h1 class="h1-p">Les produits disponibles</h1>
        </div>
        <div class="row">
            {% for produit in produits %}
                <div class="col-2">
                    <a href="{{ path('produit_show', {'id':produit.id}) }}"><img src="{{ produit.image }}" class="img-fluid h1-p shadow"></a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
